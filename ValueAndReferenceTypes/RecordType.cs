namespace ValueAndReferenceTypes
{
    //Immutable => başta değeri verildikten sonra değişmez
    //Value? => Değer tipli karşılaştırmaya izin verir
    public record RecordType(int X, int Y); // Record => verilerin taşınması için kullanılır
    

   
}